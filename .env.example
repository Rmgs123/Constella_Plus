# ОБЩЕЕ
SERVER_NAME=delta-01
LISTEN_ADDR=0.0.0.0:4747
PUBLIC_ADDR=203.0.113.10:4747

# ВЛАДЕЛЕЦ И БОТ (init-узел)
OWNER_USERNAME=@nickname
BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN

# СЕТЬ (init-узел заполняет, join-узел получит автоматически)
NETWORK_ID=
NETWORK_SECRET=

# SEED-PEERS (опционально, через запятую)
SEED_PEERS=

# JOIN_URL (ТОЛЬКО для присоединения; init-узел оставляет пустым)
JOIN_URL=
