# 🛰️ Constella

**Constella** — одноранговая сеть VPS-узлов с автоматическим выбором лидера и Telegram-ботом. Каждый сервер — равноправный участник, лидер выбирается детерминированно среди «живых» узлов, а бот обеспечивает наблюдение и управление без выделенного центрального сервера.



---

## 📈 Прогресс / Авторы вклада

- ✅ Кластер из нескольких нод с автодобавлением по join-ссылке, распределённой таблицей узлов и автоматической рассылкой новых peers через `/announce`.  
- ✅ Лидер-выбор + bot-lease: Telegram-бот гарантированно работает только на одном сервере и автоматически переезжает при падении лидера (lease RPC + Bot.Takeover).  
- ✅ Telegram-интерфейс: единое динамическое сообщение с инлайн-клавиатурой, выбором ноды, просмотром метрик, запуском спидтеста и перезагрузкой узла.  
- ✅ Сбор метрик каждые `SAMPLE_EVERY_SEC` секунд, хранение временных рядов (CPU/Network) внутри контейнера и выдача по запросу (`GetTS`).  
- ✅ Дефолтные значения для переменных окружения, упрощённая установка через `install.sh`, полностью Docker-ориентированная архитектура (privileged-контейнер для reboot).  

Вклад авторов:
- **@mvln** — подсистема метрик и временных рядов, спидтест, управление перезагрузкой хоста через контейнер (`nsenter`), хранилище состояния.  
- **@ElBruno** — Telegram-бот с инлайн-клавиатурой и постоянным сообщением, логика UI и управления командами.  

---

## 🚀 Введение / Цели проекта

Цель Constella — создать простую и устойчивую P2P-сеть серверов с автоматическим выбором лидера и Telegram-интерфейсом для мониторинга и управления. Новые узлы подключаются по одноразовому токену (join-link), лидер определяется автоматически и переносит бот вместе с собой.

**Ключевые возможности MVP:**

- Автоматический выбор и смена лидера («Хост») по `priority` + `node_id`.
- Подключение новых узлов по одноразовому токену с TTL.
- Telegram-бот для просмотра метрик, спидтеста и перезагрузки узлов.
- Работа без центрального сервера: все узлы равноправны, состояние распространяется автоматически.

---

## 🧭 Область проекта (Scope)

### Что входит в MVP

- P2P-членство и heartbeat на основе `/health`, локального списка peers и seed-узлов.
- Автовыбор лидера среди «живых» узлов по ключу `(priority, node_id)`; лидер запускает Telegram-бота.
- Bot lease и `Bot.Takeover`, чтобы бот работал только у лидера и переезжал при смене.
- Join по токену через `/join` (TTL, одноразовое использование) и `/join_handshake`.
- Просмотр списка узлов с пометкой лидера, статусов online/offline.
- Запрос метрик (CPU, RAM, диск, аптайм), временных рядов и удалённая перезагрузка узла.

### Что не входит в MVP

- Смена владельца сети через UI (только ручное редактирование state).
- Полноценный NAT-traversal без публичных узлов.
- Полносвязный VPN-меш “каждый-с-каждым” (сейчас — режим хаб/клиент).
- Графическая веб-панель — управление только через Telegram-бота.

---

## 🧩 Основные сценарии использования (Use Cases)

1. **Инициализация сети.** Первый узел запускается через `install.sh init` с указанием публичного адреса и Telegram-ника владельца. Скрипт создаёт `state/`, `network_id` и `network_secret`.  
2. **Выдача приглашения.** Команда `/invite <ttl>` в боте создаёт одноразовую join-ссылку; запись сохраняется в `state/invites.json`.  
3. **Подключение нового узла.** На новом сервере выполняется `install.sh join --join "<link>"`, после чего контейнер автоматически вызывает `/join` на сид-узле и получает состояние сети.  
4. **Просмотр списка узлов.** Бот показывает имена, `node_id` (укороченно), статус (🟢/🔴) и помечает текущего лидера как «Хост».  
5. **Запрос метрик.** Из карточки узла бот вызывает `GetStats` и `GetTS`, отображая CPU/RAM/диск, аптайм и усреднённые CPU/Network срезы за последние часы.  
6. **Спидтест.** Кнопка запускает RPC `RunSpeedtest` на целевом узле; результаты (down/up/ping) добавляются в временные ряды.  
7. **Перезагрузка узла.** Кнопка подтверждения инициирует `Reboot` RPC; контейнер вызывает `nsenter` к PID 1 и перезапускает хост.  
8. **Смена лидера.** При недоступности лидера heartbeat помечает его как down, выбирается новый лидер, он получает bot lease и перезапускает бота.

---

## ⚙️ Требования

### Функциональные

- P2P-членство с heartbeat и `/health` проверками каждые `HEARTBEAT_INTERVAL` секунд.
- Детерминированный выбор лидера по `LeaderKey=(priority, node_id)` среди «живых» узлов.
- Telegram-бот запускается только у владельца bot lease; lease обновляется через RPC.
- Join по одноразовому токену (`invites.json`) с TTL и валидацией `network_id`.
- RPC-методы `/rpc`: `GetPeers`, `GetStats`, `Reboot`, `GetTS`, `RunSpeedtest`, `Join`, `Lease.*`, `Bot.Takeover`.
- Доступ к боту ограничен владельцем `@owner_username` (все команды проверяют username).

### Нефункциональные (целевые)

- Время failover лидера ~10 секунд (heartbeat + lease).
- Простая установка: `git clone`, `install.sh`, `docker compose up -d --build`.
- Сетевая подпись RPC — HMAC по `NetworkSecret`; TLS пока планируется.
- Совместимость Linux x86_64/ARM64, контейнер требует `privileged` и `pid: host`.
- Низкое потребление ресурсов: сбор метрик по расписанию, лёгкий aiohttp-сервер.

---

## 🏗️ Архитектура

- **Membership & Heartbeat.** Каждый узел опрашивает peers по `/health`, обновляет `state["peers"]`, помечает `alive`/`down` и кэширует в `state/network_state.json`.  
- **RPC-API.** Один HTTP-эндпоинт `/rpc` принимает подписанные HMAC JSON-запросы, выполняет команды и управляет lease.  
- **Leader Services.** Функция `current_leader()` определяет лидера; только лидер держит bot lease и запускает Telegram-поллинг. При смене лидера рассылается `Bot.Takeover`.  
- **Данные метрик.** Фоновый таск сохраняет CPU/Network `deque`, чтобы быстро отдавать графики и спидтесты.  

Поток команд: пользователь → Telegram-бот (лидер) → RPC к нужному узлу → ответ → редактирование динамического сообщения.

---

## 🌐 Сеть и протоколы

- **Порт по умолчанию:** `4747/TCP` (`LISTEN_ADDR`).  
- **Подпись RPC:** HMAC SHA256 (`verify_sig`) на основе `NetworkSecret`.  
- **Heartbeat:** период `HEARTBEAT_INTERVAL` (по умолчанию 2 с), узел считается down после `DOWN_AFTER_MISSES` пропусков и `HEARTBEAT_TIMEOUT`.  
- **Discovery:** начальные seed-адреса (`SEED_PEERS`) + автообновление peer-таблицы при join через `/announce` и `/join`.  
- **Лидерство:** `current_leader()` возвращает кандидата с минимальным `(priority, node_id)`, включая локальный узел.  

---

## 🔑 Подключение (Join)

**Формат ссылки:**

```
join://<seed-host>:4747?net=<NetworkID>&token=<OneTimeToken>&ttl=<15m>
```

**Процесс:**

1. Бот `/invite` создаёт запись `{token, exp_ts}` в `invites.json`.  
2. Новый узел запускает `install.sh join`, который сохраняет `JOIN_URL` и стартует контейнер.  
3. При первом запуске контейнер вызывает `/join` на seed: передаёт `server_name`, токен, `network_id`, `public_addr`.  
4. Seed проверяет токен и TTL, возвращает `network_id`, `owner_username`, `network_secret`, актуальный список peers.  
5. Новый узел сохраняет `state/network_state.json`, регистрирует себя и начинает heartbeat.  

---

## 💬 Telegram-интерфейс

- Одно динамическое сообщение с инлайн-клавиатурой: список узлов с пометкой лидера («Хост») и статусами 🟢/🔴.  
- При выборе узла отображается карточка: CPU/RAM/диск, аптайм, усреднённые CPU/Network графы, последняя скорость, кнопки `Speedtest`, `Reboot`.  
- `/invite <ttl>` — выдаёт join-ссылку с заданным временем жизни.  
- Бот отвечает только владельцу `@owner_username`; остальные пользователи получают отказ.  

---

## 🧠 RPC API (кратко)

| Метод | Назначение |
| --- | --- |
| `GetPeers` | Возвращает список узлов `{name, node_id, addr, status, last_seen}`. |
| `GetStats` | Отчёт по CPU/RAM/диску и аптайму выбранного узла. |
| `Reboot` | Инициирует перезагрузку узла через `nsenter` → `/sbin/reboot`. |
| `GetTS(kind=cpu\|net, hours)` | Возвращает временные ряды CPU или сетевых скоростей. |
| `RunSpeedtest` | Запускает локальный speedtest-cli, добавляет результаты в `NET_*_SAMPLES`. |
| `Join` | Используется сид-узлом для приёма новых peers. |
| `Lease.Get/Acquire/Release` | Управление bot lease у координатора (обычно лидера). |
| `Bot.Takeover` | Уведомление других узлов о смене владельца бота. |

---

## 📂 Хранение состояния

- `state/network_state.json` — основное состояние узла: `network_id`, `owner_username`, `network_secret`, массив `peers[]` с адресами и `node_id`.  
- `state/invites.json` — список активных одноразовых токенов (`tokens[]` с `exp_ts`).  
- `state/node_id` — уникальный идентификатор узла (SHA256 по имени + UUID).  
- `state/wg/` — файлы WireGuard (если включён VPN).  

Состояние сохраняется между рестартами контейнера; удаление каталога `state/` обнуляет сетевую привязку.

---

## 🔐 VPN overlay (план / экспериментальная часть)

WireGuard-overlay позволяет прокидывать трафик Constella по приватной подсети. Поддерживаются режимы:

- `VPN_MODE=none` (по умолчанию) — VPN отключён.
- `VPN_MODE=hub` — публичный VPS поднимает WireGuard (`VPN_INTERFACE_NAME`, `VPN_CIDR`, `VPN_LISTEN_PORT`). Скрипт генерирует ключи и конфиг, но добавление peers пока вручную.
- `VPN_MODE=client` — узел за NAT подключается к хабу (`VPN_ADDRESS`, `VPN_HUB_ENDPOINT`, `VPN_HUB_PUBLIC_KEY`, опционально `VPN_ALLOWED_IPS`, `VPN_PRESHARED_KEY`).

Приложение обнаруживает IP интерфейса (`detect_vpn_ip`) и, если `PUBLIC_ADDR` не задан вручную, публикует адрес вида `<vpn_ip>:4747`. Overlay пока **экспериментальный**: требуется ручное управление `wg-quick` и добавлением peers; полноценный mesh и автоматическое распределение ключей находятся в планах развития.




---

## 🛠️ Развёртывание и команды

### Предусловия

- Linux-сервер с Docker, docker compose plugin и root-доступом.
- Telegram Bot Token и @username владельца.
- (Опционально) WireGuard (`wg`, `wg-quick`, `ip`) для VPN режима.

---

### 1. Первый (seed) публичный узел

```bash
git clone https://github.com/Rmgs123/Constella_Plus.git
cd Constella_Plus

chmod +x install.sh

apt install -y ufw
ufw allow 22/tcp
ufw allow 4747/tcp
ufw allow 51820/udp
ufw enable
ufw status

./install.sh init \
  --server-name delta-01 \
  --public-addr 203.0.113.10:4747 \
  --owner @username \
  --bot-token 123456789:ABCDEF-your-telegram-bot-token

docker compose up -d --build
docker compose logs -f
```

---

### 2. Второй публичный узел (ещё один VPS)

```bash
git clone https://github.com/Rmgs123/Constella_Plus.git
cd Constella_Plus

chmod +x install.sh

apt install -y ufw
ufw allow 22/tcp
ufw allow 4747/tcp
ufw allow 51820/udp
ufw enable
ufw status

./install.sh join \
  --server-name echo-02 \
  --join "join://203.0.113.10:4747?net=abcd1234&token=XYZ&ttl=15m"

# Можно указать BOT_TOKEN здесь или позже через .env
docker compose up -d --build
docker compose logs -f
```

---

### 3. Домашний узел за NAT (через VPN-overlay)

**Хаб (публичный VPS):**

```bash
apt update
apt install -y wireguard wireguard-tools

./install.sh init \
  --server-name hub-01 \
  --public-addr 203.0.113.10:4747 \
  --owner @username \
  --bot-token 123:ABC \
  --vpn-mode hub \
  --vpn-cidr 10.42.0.0/24 \
  --vpn-listen-port 51820 \
  --vpn-address 10.42.0.1/24

(После установки клиента - добавить)
nano state/wg/wg0.conf

[Peer]
PublicKey = MzorjlCwWxKEnx0sgyfEdWP2l/M4rh7slI9eP0l31SA=
AllowedIPs = 10.42.0.2/32
PersistentKeepalive = 25
```

Скрипт создаст `state/wg/wg0.conf`, сгенерирует ключи и выведет публичный ключ хаба; добавляйте клиентов вручную в `[Peer]`.

**Клиент (домашний ПК):**

```bash
apt update
apt install -y wireguard wireguard-tools

./install.sh join \
  --server-name home-01 \
  --join "join://203.0.113.10:4747?net=abcd1234&token=XYZ&ttl=15m" \
  --vpn-mode client \
  --vpn-address 10.42.0.2/32 \
  --vpn-hub-endpoint 203.0.113.10:51820 \
  --vpn-hub-public-key <hub-public-key>
```

Скрипт сохранит `state/wg/wg0.conf`, покажет публичный ключ клиента. После добавления на хаб можно поднять интерфейс (`wg-quick up state/wg/wg0.conf`). Constella автоматически начнёт использовать VPN-адрес для RPC.

---

### 4. Сеть только из домашних ПК (ограничения)

Полностью закрытая сеть из NAT-клиентов без публичного адреса **не поддерживается**: нужен хотя бы один узел с проброшенным портом (`hub`), через который остальные клиенты подключатся по WireGuard.

---

### 5. Запуск и управление контейнерами

```bash
docker compose up -d --build
docker compose logs -f
```

Перезапуск:

```bash
docker compose down
docker compose up -d --build
```

---

### 6. Обновление кода из Git

```bash
docker compose down
git pull
docker compose build --no-cache   # опционально
docker compose up -d
```

---

### 7. Очистка / удаление

```bash
docker compose down
docker compose down -v            # удаляет volumes
docker image prune -a
docker volume prune
docker system prune -a --volumes

wg-quick down wg0 || true
apt remove --purge -y wireguard wireguard-tools
rm -rf /etc/wireguard
rm -rf /usr/lib/modules/*/kernel/net/wireguard 2>/dev/null || true

cd ~/Constella_Plus
rm -rf state/wg

cd ..
rm -rf Constella_Plus             # удалит state/ и всю сетевую информацию
```

> ⚠️ Удаление каталога `state/` уничтожает `network_secret`, `node_id`, invites и WireGuard-конфиги — сеть придётся инициализировать заново.

---

## 📌 Итоги / планы развития

- Дальнейшая работа над VPN overlay: автоматическое добавление peers, mesh-маршруты и шифрование RPC поверх туннеля.
- Улучшение NAT-traversal (STUN/TURN) для сетей без публичных адресов.
- Расширенные метрики (IOPS, температурные датчики), новые графики и экспорт в Prometheus.
- Возможная веб-панель поверх текущего API и дополнительные роли пользователей.

Constella уже предоставляет минимально жизнеспособный одноранговый кластер с Telegram-управлением; развитие сосредоточено на надёжности сети, расширении метрик и упрощении подключения домашних узлов.
